# Environment Variables Template
# Copy this file to frontend/.env.local and fill in your values

# Admin Account Credentials
# Used by scripts/create-admin.js to create the admin user
ADMIN_EMAIL=admin@despendables.com
ADMIN_PASSWORD=ChangeThisToASecurePassword123

# Note: .env.local is gitignored and should never be committed
# Keep these credentials secure and change the default password
